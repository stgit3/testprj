pipeline {
    agent  any
    stages {
        stage('build') {
            steps {
                echo  'building the applications'
            }
        }
		stage('Test') {
		    steps {
			   echo 'Testing the application'
			   }
			}
       stage('deploy') {
           steps {
            echo 'deploying the application'
          
          }
      }
    }
}
